{
  "name": "composio-orchestrator",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Universal Composio v3 auth orchestrator for cross-agent (Claude, Gemini, Codex) deployment via ~/.dotfiles.",
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "orch": "tsx bin/composio-orch.ts"
  },
  "dependencies": {
    "@composio/core": "^0.8.1",
    "openai": "^6.35.0",
    "undici": "^6.0.0",
    "zod": "^4.0.0"
  },
  "optionalDependencies": {
    "@composio/anthropic": "*",
    "@composio/langchain": "*",
    "@composio/openai": "^0.8.1",
    "@composio/openai-agents": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^4.1.6",
    "tsx": "^4.22.4",
    "typescript": "^5.4.0",
    "vitest": "^4.1.5"
  }
}
